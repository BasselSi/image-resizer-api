# Final Project (fresh scaffold)

This is a freshly scaffolded Python project.

How to run locally:

1. Create and activate the virtual environment

   python3 -m venv .venv
   source .venv/bin/activate

2. Install dependencies

   pip install -r requirements.txt

3. Run the app

   python -m src.app

Run tests:

    pytest -q
